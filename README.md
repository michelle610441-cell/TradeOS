# TradeOS V2.7.1

针对 V2.7 的统计页打不开问题做了专门修复：

- 统计页改为防御式读取旧数据，缺少 ledger / accountSnapshot / trades 等字段时不会崩溃
- 统计计算不再依赖 Array.flatMap
- equityPoints 对旧记录做空值兼容
- 历史交易仍可编辑实际收益率、修改模式、删除
- 如果未来遇到单条异常数据，统计页会显示错误信息而不是整页空白
- 保留 V2.7 的持仓延续、账户流水、账户快照和权益曲线
